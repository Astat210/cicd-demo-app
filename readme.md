# CI/CD Demo Application

Демонстрационное приложение для изучения CI/CD pipeline с автоматическим деплоем.

## 🚀 Описание проекта

Простой REST API на Node.js/Express с полным CI/CD pipeline:
- Автоматическое тестирование
- Сборка Docker образа
- Автоматический деплой в облако

## 📋 Требования

- Node.js 18+
- npm или yarn
- Git
- Docker (опционально)

## 🛠️ Установка и запуск

### Локальная разработка

```bash
# Клонировать репозиторий
git clone <your-repo-url>
cd cicd-demo-app

# Установить зависимости
npm install

# Запустить в режиме разработки
npm run dev

# Запустить в production режиме
npm start
```

### Тестирование

```bash
# Запустить все тесты
npm test

# Запустить тесты с coverage
npm test -- --coverage

# Запустить тесты в watch режиме
npm run test:watch
```

### Docker

```bash
# Собрать образ
docker build -t cicd-demo-app .

# Запустить контейнер
docker run -p 3000:3000 cicd-demo-app
```

## 📡 API Endpoints

### Основные

- `GET /` - Информация о приложении
- `GET /health` - Проверка состояния сервиса

### Пользователи

- `GET /api/users` - Получить всех пользователей
- `GET /api/users/:id` - Получить пользователя по ID
- `POST /api/users` - Создать нового пользователя

### Примеры запросов

```bash
# Получить всех пользователей
curl http://localhost:3000/api/users

# Создать пользователя
curl -X POST http://localhost:3000/api/users \
  -H "Content-Type: application/json" \
  -d '{"name":"Иван","email":"ivan@example.com"}'
```

## 🔄 CI/CD Pipeline

### GitHub Actions Workflow

Pipeline состоит из 4 этапов:

1. **Test & Build** - Установка зависимостей и запуск тестов
2. **Docker** - Сборка Docker образа (для main ветки)
3. **Deploy** - Автоматический деплой в облако
4. **Notify** - Уведомления о статусе pipeline

### Триггеры

- Push в ветку `main` - полный pipeline с деплоем
- Pull Request - только тестирование

## ☁️ Деплой

### Render.com

1. Создайте аккаунт на [Render.com](https://render.com)
2. Создайте новый Web Service
3. Подключите GitHub репозиторий
4. Настройте параметры:
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Auto-Deploy**: Yes
5. (Опционально) Добавьте Deploy Hook в GitHub Secrets как `RENDER_DEPLOY_HOOK_URL`

### Railway.app

1. Создайте аккаунт на [Railway.app](https://railway.app)
2. Нажмите "New Project" → "Deploy from GitHub repo"
3. Выберите ваш репозиторий
4. Railway автоматически определит настройки для Node.js
5. Включите автоматический деплой для main ветки

## 📊 Структура проекта

```
cicd-demo-app/
├── .github/
│   └── workflows/
│       └── deploy.yml       # GitHub Actions workflow
├── index.js                 # Основной файл приложения
├── app.test.js             # Тесты
├── package.json            # Зависимости и скрипты
├── Dockerfile              # Docker конфигурация
├── .dockerignore          # Исключения для Docker
├── jest.config.js         # Конфигурация Jest
├── .gitignore             # Исключения для Git
└── README.md              # Документация
```

## 🧪 Покрытие тестами

Проект включает unit и integration тесты для всех endpoints:
- ✅ GET запросы
- ✅ POST запросы
- ✅ Обработка ошибок
- ✅ 404 обработчик

## 🔐 Переменные окружения

```bash
PORT=3000  # Порт сервера (опционально)
```

## 📈 Мониторинг

- `/health` endpoint для health checks
- Docker HEALTHCHECK встроен в образ
- Логи доступны через `docker logs` или облачный dashboard

## 🤝 Разработка

### Внесение изменений

1. Создайте новую ветку: `git checkout -b feature/new-feature`
2. Внесите изменения
3. Запустите тесты: `npm test`
4. Сделайте commit: `git commit -m "Add new feature"`
5. Push в GitHub: `git push origin feature/new-feature`
6. Создайте Pull Request

### После merge в main

1. GitHub Actions автоматически запустит pipeline
2. Выполнятся все тесты
3. Соберется Docker образ
4. Приложение автоматически задеплоится в облако

## 📝 Лицензия

MIT

## 👨‍💻 Автор

Ваше имя

---

**Статус CI/CD**: ![CI/CD Pipeline](https://github.com/yourusername/cicd-demo-app/workflows/CI/CD%20Pipeline/badge.svg)